# Initech MCS synthetic repo-scale notes

This is a small, bounded stand-in for a scale-strain profile. It deliberately avoids multi-GB content and large repo counts. The safe cap is controlled by MCS_SCALE_REPO_CAP and must stay between 1 and 5.
